# Build artifacts — never commit
data-raw/_targets/
data-raw/_targets_change002_delta/
data-raw/logs/
data-raw/*.log
data-raw/slurm-*.out

# MET-08: informal internal working notes -- excluded here so a fresh git init
# (MET-09, publishing this directory as cancer-genomics/hallberg2025.meth.data) never
# picks them up. Stay tracked in the private outer repo; not untracked here.
data-raw/step4_inputs_locator_results.txt
notes_shashi.org
data-raw/locate_step4_inputs.sh

# DOC-13: AI assistant context files are internal (lab SOP, sop.reproducibility ->
# release.qmd, "AI assistant context files stay internal"). No leading slash, so this
# also catches data-raw/CLAUDE.md. Both remain tracked in the private outer repo.
CLAUDE.md
